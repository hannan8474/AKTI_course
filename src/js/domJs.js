const jsClass = document.querySelectorAll('p');

for (let i = 0; i < jsClass.length; i++) {
    const element = jsClass[i];
    // Change the background color for each element
    element.style.backgroundColor = 'lightblue';
}
// const jsGeneral = document.querySelectorAll('.js')
// console.log(jsGeneral)